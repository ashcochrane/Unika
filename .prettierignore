# --- Shopify-generated / Shopify-owned. Never format these. ---
# These files are written by the theme editor and synced back from the admin.
# Formatting them creates permanent churn: Shopify rewrites, Prettier reformats,
# forever. Verified: Prettier wants to reformat 38 of them.
templates/
config/
locales/
sections/*.json

# --- Vendored + Dawn stock assets, and build output ---
# We don't hand-author these; reformatting makes Dawn upgrades merge-hostile.
assets/

# Licence text — never reformat.
LICENSE.md

node_modules/
package-lock.json
